<section class="container d-flex flex-column align-items-center gap-3">

    <div id="y-img" class="d-flex flex-column align-items-center gap-3">
        
    </div>

    <div class="d-flex flex-column align-items-center gap-5">

        <div id="y-corp" class="d-flex flex-column align-items-center gap-3 w-100">
            
        </div>

        <div class="d-flex flex-column align-items-center">
            <span class="fw-bold">Category</span>
            <ul id="y-category" class="p-0 m-0">
                @*@foreach (Category category in Model.Category)
                {
                    
                }*@
            </ul>
        </div>

    </div>

</section>

@section Scripts {
    <script type="text/javascript" src="~/js/Client/show.js"></script>
}